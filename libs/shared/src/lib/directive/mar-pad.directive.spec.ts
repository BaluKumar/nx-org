import { MarPadDirective } from './mar-pad.directive';

describe('MarPadDirective', () => {
  it('should create an instance', () => {
    const directive = new MarPadDirective();
    expect(directive).toBeTruthy();
  });
});
