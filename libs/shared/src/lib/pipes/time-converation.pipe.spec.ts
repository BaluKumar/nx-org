import { TimeConverationPipe } from './time-converation.pipe';

describe('TimeConverationPipe', () => {
  it('create an instance', () => {
    const pipe = new TimeConverationPipe();
    expect(pipe).toBeTruthy();
  });
});
